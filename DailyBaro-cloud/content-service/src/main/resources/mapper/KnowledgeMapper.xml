<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailybaro.content.knowledge.mapper.KnowledgeMapper">

    <resultMap id="BaseResultMap" type="com.dailybaro.content.knowledge.model.Knowledge">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="category" property="category"/>
        <result column="subcategory" property="subcategory"/>
        <result column="tags" property="tags"/>
        <result column="vector_id" property="vectorId"/>
        <result column="status" property="status"/>
        <result column="view_count" property="viewCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
    </resultMap>

    <insert id="insert" parameterType="com.dailybaro.content.knowledge.model.Knowledge" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge
            (title, content, category, subcategory, tags, vector_id, status, view_count, create_time, update_time, create_by)
        VALUES
            (#{title}, #{content}, #{category}, #{subcategory}, #{tags}, #{vectorId}, #{status}, 0, NOW(), NOW(), #{createBy})
    </insert>

    <update id="update" parameterType="com.dailybaro.content.knowledge.model.Knowledge">
        UPDATE knowledge
        SET title = #{title},
            content = #{content},
            category = #{category},
            subcategory = #{subcategory},
            tags = #{tags},
            vector_id = #{vectorId},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM knowledge WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM knowledge WHERE id = #{id}
    </select>

    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        WHERE category = #{category} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectBySubcategory" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        WHERE category = #{category} AND subcategory = #{subcategory} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        ORDER BY create_time DESC
    </select>

    <select id="searchByKeyword" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        WHERE status = 1 
        AND (title LIKE CONCAT('%', #{keyword}, '%') 
             OR content LIKE CONCAT('%', #{keyword}, '%')
             OR tags LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY view_count DESC, create_time DESC
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT * FROM knowledge 
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND status = 1
        ORDER BY FIELD(id,
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </select>

    <update id="incrementViewCount">
        UPDATE knowledge SET view_count = view_count + 1 WHERE id = #{id}
    </update>

</mapper>
